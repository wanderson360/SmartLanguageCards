<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_slc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_slc_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThemeStylesIcons">
        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:parentActivityName=".WordListActivity" />
        <activity
            android:name=".WordListActivity"
            android:exported="true"
            android:launchMode ="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ProgressoActivity"
            android:exported="false"
            android:parentActivityName=".ProgressoActivity"
            />
        <activity
            android:name=".WordActivity"
            android:exported="false"
            android:parentActivityName=".WordListActivity"
        />
        <activity
            android:name=".RandomCardActivity"
            android:exported="false"
            android:parentActivityName=".RandomCardActivity"
        />
    </application>

</manifest>